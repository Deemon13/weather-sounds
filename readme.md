A small application that plays nature sounds (three sounds) and adjusts the volume of the played sound. The application is built using the Webpack ver. 5.0 compiler and is written in vanilla JavaScript.

The main stack includes Javascript, HTML, CSS (SCSS), Webpack, and GIT.
